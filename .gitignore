__pycache__/
*.py[cod]
.venv/
venv/
env/

# tool output (one folder per analyzed video)
*_watch/

# web app: saved API key (local only), gradio cache, and per-run output
.vw_key
.gradio/
runs/

# local scratch
_smoke/
_smoke2/
