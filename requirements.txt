# Python dependencies for video_watcher.py
# System dependency (install separately): ffmpeg + ffprobe
#   Ubuntu/Debian:  sudo apt install ffmpeg
#   macOS:          brew install ffmpeg
#   Windows:        winget install Gyan.FFmpeg   (or choco/scoop install ffmpeg)

anthropic>=0.40.0
faster-whisper>=1.0.0

# Optional: only needed to pass a TikTok/YouTube/Instagram URL instead of a
# local file. Install with: pip install -U yt-dlp
# yt-dlp>=2024.1.0
